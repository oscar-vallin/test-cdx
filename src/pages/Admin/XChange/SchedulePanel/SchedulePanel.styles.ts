import { Text } from '@fluentui/react';
import styled from 'styled-components';

export const StyledXchanges = styled.ul`
    margin-left: 50px;
    & li {
        margin-top: 5px;
        font-weight: 600;
        font-size: 15px;
    }
`;

export const StyledText = styled(Text)`
  font-size: 10px;
  margin-bottom: 5px;
`;

import { ThemedCommandButton } from 'src/components/buttons/Button/Button.styles';
import styled from 'styled-components';

export const StyledThemedCommandButton = styled(ThemedCommandButton)`
    .ms-Icon {
        font-size: 20px;
    }
`;

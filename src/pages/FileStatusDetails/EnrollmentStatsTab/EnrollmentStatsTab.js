import React from 'react';
import { Spacing } from '../../../components/spacings/Spacing';

const EnrollmentStatsTab = () => {
  return <Spacing padding="normal">
    {/* <Table
      items={tableData}
      structure={getTableStructure(tableId)}
    /> */}
  </Spacing>
}

export default EnrollmentStatsTab;